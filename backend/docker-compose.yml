version: "3.8"

services:
    backend-app:
        build: .
        container_name: debug-backend-app
        ports:
            - "4322:4321"
        environment:
            DB_HOST: "host.docker.internal"
            DB_PORT: "3306"
        extra_hosts:
            - "host.docker.internal:host-gateway"
